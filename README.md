# Drag-in Google Photos Takeout repair (JSON sidecar → EXIF, dedupe, rename)

Live: https://takeout-photo-metadata-fixer.sociobot.in — built by the Param Factory (`pwa-offline`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
